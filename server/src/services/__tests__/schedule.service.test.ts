import {describe,expect,it} from 'vitest';import {allocateSchedule} from '../schedule.service.js';describe('schedule',()=>{it('creates exactly requested days and assigns every question',()=>{const r=allocateSchedule([{id:'q1',requirement_ids:['r1'],difficulty:3},{id:'q2',requirement_ids:['r2'],difficulty:1},{id:'q3',requirement_ids:['r1'],difficulty:2}],[{id:'r1',priority:'must'},{id:'r2',priority:'nice'}],2);expect(r.days).toHaveLength(2);expect(r.days.flatMap(d=>d.question_ids).sort()).toEqual(['q1','q2','q3']);expect(r.days.every(d=>Number.isInteger(d.minutes))).toBe(true)})});
